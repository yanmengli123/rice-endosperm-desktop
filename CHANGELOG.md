# 更新日志

## 0.3.6（2026-08-28）

- 修复：跨异步 Context 的 MCP 执行上下文 Token 异常不再泄漏进桌面端回答（服务端 MCP 上下文 / BYOK 凭据上下文都改为跨 Context 安全的 no-op reset）。这是上一版本偶发的"\"Yuxi 返回了无法识别的数据\"\"的根根因。
- 修复：切换会话或新建会话后上一次回答的答案不再消失——消息表改为 append-only（同 id 重复写入不会覆盖已有答案），并按 account_scope 隔离跨账号数据；服务端生成或抢救回来的有效回答会先于返回落库，桌面端不会丢弃。
- 修复：MCP 审计摘要不再使用 ``default=str`` 兜底序列化，对 Token / ContextVar / callable 一律拒绝进入 digest 与日志。
- 修复：服务端 invalid_agent 早退路径的上下文残留——彻底交给生成器 finally 统一清理，杜绝同 token 二次 reset。

## 未发布

- 设置页新增用户专属大模型配置：可手动填写 OpenAI/Anthropic 兼容协议、HTTPS API Base URL、API Key 和 model，也可粘贴 Claude Code 风格 JSON 一键导入；保存后同步设为服务端用户默认模型。
- JSON 仅提取模型调用必需的三项配置，其他环境变量不会执行或落盘；API Key 不写本机 SQLite/日志，仍由 Yuxi 服务端按账号加密保存并仅回显掩码。

## 0.3.5（2026-08-28）

- 设置页新增用户专属大模型配置（OpenAI/Anthropic 兼容 + Claude Code JSON 一键导入），保存即同步为服务端用户默认模型。API Key 不落本机 SQLite/日志，仍由 Yuxi 服务端按账号加密保存。

## 0.3.4（2026-08-27）

- 桌面问答切换为与 Web 端完全相同的原生 Conversation + AgentRun v1.2 契约：服务端权威选择默认智能体、冻结模型/知识范围/请求身份，并提供最终回答；桌面端只负责提交问题、展示服务端事件和持久化服务端最终结果。
- 新建桌面对话使用客户端线程 ID 幂等创建服务端会话，避免超时重试产生孤儿线程；服务端返回的智能体、线程、请求 ID 与结果权威标记任一不一致时客户端将 fail-closed，不再展示可能串线的回答。
- 新桌面对话会进入 Yuxi 的正常会话体系，不再标记为外部 `agent_call`，因此与 Web 端共享同一历史、运行观测、知识检索审计和结果接口。

## 0.3.3（2026-08-27）

- 模型推理过程改为不可见的运行状态：流式响应、最终结果和本地历史均会过滤 `<think>`、转义标签及未闭合标签，界面仅显示固定的“思考中…”，不再渲染或持久化大模型内部推理文本。

## 0.3.2（2026-08-27）

- 三要素登录改由服务端原子完成：登录 ID + 初始密码 + API Key 由 Yuxi 的 `POST /api/auth/desktop/login` 单事务联合校验账号状态、密钥归属及租户/部门边界，客户端不再分别请求 token / me 后自行拼接身份；服务端固化的 `account_scope_id` 作为本地隔离真源，统一为 `网关|作用域` 格式以修复多账号切换时作用域重复拼接或凭据失联；连接页登录成功后立即进入主界面。

## 0.3.1（2026-08-26）

- 新增三字段登录：连接页输入管理员发放的登录名 + 初始密码 + API Key，客户端会验证密码对与密钥属主一致后才绑定本机。

## 0.3.0（2026-08-26）

- 新增「我的模型密钥」（BYOK）：设置页可选择供应商并录入自己购买的厂商 API Key，服务端加密保存、界面仅显示掩码；命中供应商的对话优先使用你的密钥，移除后自动回到企业模型轨道。
- 密钥仅经 Rust 层请求发送到服务端加密存储，不落 SQLite、不写日志；移除为逻辑撤销，历史用量仍可在 usage_ledger 对账。

## 0.2.0（2026-08-25）

- 新增激活码登录：管理员开户后发放一次性激活码，在连接页输入即可绑定账号并使用安全会话（无静态 API Key，凭证仅存安全存储）。
- 会话安全收紧：会话型账号的刷新失败不再回退静态 API Key，统一提示重新登录，确保管理员下线设备立即生效。
- 连接状态判定兼容纯会话账号；设置页多账号切换/移除延续 v0.1.9。

## 0.1.9（2026-08-25）

- 多账号支持：设备码登录的每个账号在 Stronghold 中拥有独立凭据与会话存储，设置页可查看、切换与移除账号；切换不再互相覆盖。
- 接入服务端旋转会话：优先使用 30 分钟短时访问令牌调用网关，临近过期自动用刷新令牌轮换（重放保护由服务端会话族承担），失败自动回落过渡 API Key。
- 本地数据库升级到迁移版本 5（新增 accounts 账号目录），历史会话按账号作用域隔离不变。
- 与服务端 P2 会话协议对齐：exchange 响应中的 session 字段可选解析，旧服务端自动回退 Key 流。

## 0.1.8（2026-08-24）

- 新增设备码登录：连接页发起 CLI 授权会话并拉起浏览器审批页，批准后自动换取 API Key 完成绑定，无需手工复制长密钥。
- 多账号协议：本地数据库升级到迁移版本 4（threads 增加 account_scope），会话数据按账号范围隔离，保留全部历史会话。
- 创建任务支持可选 model_spec：设备登录后自动采用服务端用户级模型偏好，未显式指定时由服务端按「请求级 > 智能体级 > 用户级 > 系统级」解析。

## 0.1.7（2026-08-22）

- 修复取消竞态：本地取消不再提前写入终态，改为 `cancel_requested` 并由结果对账收敛，服务端已完成的完整回答不再因晚到取消而永久丢失。
- 修复 RunEvent 序列化字段命名断裂：前端恢复轮询现在能正确拿到 run 标识，客户端侧失败后的答案找回不再需要重启应用。
- 恢复同步与存活流增加终态守卫，非终态写入不得降级已终态行，消除"已完成被改回运行中"的僵尸状态。
- 创建任务遇到网络类失败时借助 request_id 幂等自动重试一次，减少服务端孤儿任务。
- 非合法 JSON 的 SSE 帧（心跳、代理注入）跳过处理并记录日志，不再终止整次对话。
- 系统凭据库中的解锁密钥被清理后，应用会备份旧快照并重建安全存储，可自愈启动而不再陷入启动死局。
- 新增单实例保护，避免双实例竞争安全存储快照与更新器。
- 轮询期取消保留已流式输出的部分回答；消息位置改为原子分配；时间戳统一毫秒精度；网关校验支持 IPv6 回环。
- API Key 缓冲全程仅一份明文并在丢弃时清零。
- 会话界面：切换会话后旧任务迟到完成事件不再覆盖新会话上下文条；流式输出期间恢复轮询不再重载会话；打开会话先加载数据再切换，失败提供重试入口。

## 0.1.6（2026-08-21）

- 修复服务端已完成但最终正文短暂不可见时，桌面端误将任务移出恢复队列的问题。
- 待恢复任务现在会按原 `run_id` 持续轮询；临时网络故障会自动重试，达到安全阈值后给出明确诊断且不会重复提交问题。
- 修复服务端知识检索状态大小写不同导致桌面端未显示告警的问题。
- 本地数据库升级到迁移版本 3，保留全部历史会话、消息与运行上下文。

## 0.1.5（2026-08-21）

- 严格使用 Yuxi AgentRun 最终 `output` 作为问答结果，并展示服务端实际模型、冻结知识范围和检索 Claim/Evidence 统计。
- 适配 Yuxi `run_context` 1.1 协议，运行上下文按会话持久化，切换或重启桌面端后仍可核验本次问答使用的知识库。
- 新增未完成任务恢复：桌面端异常退出后按原 `run_id` 查询并回填服务端最终答案，不重新创建任务。
- 正确解析服务端结构化错误消息和错误类型，人工中断不再被误报为用户取消。
- 本地数据库升级到迁移版本 2，保留全部历史会话与消息。

## 0.1.4（2026-08-21）

- 修复 Tauri Channel 最终事件与命令返回值之间的时序竞争，始终以 Yuxi 服务端返回的权威 `output` 更新最终回答。
- 流式预览按 Yuxi `message_id` 隔离多轮模型消息，避免把知识检索规划、工具调用过程和最终回答拼接成乱码。
- 桌面端只渲染 `message_delta.content`，不展示 `reasoning_content`、工具参数或子线程内容。
- Yuxi 已完成但最终消息仍在落库时会短暂重试，不再退回内部流式文本作为最终答案。

## 0.1.3（2026-08-21）

- 适配最新版 `rice-endosperm-agent` 的思考模型多轮工具调用协议，修复 DeepSeek 等模型因缺少 `reasoning_content` 回传而导致的 400 错误。
- 增加旧版 Yuxi 服务端兼容性检测：异常响应不会再作为正常回答写入本地聊天记录，并会给出可执行的服务端升级提示。
- 服务端模型兼容性修复已同步至 `yanmengli123/rice-endosperm-agent` 的 `main` 分支。

## 0.1.2

- 完善 Windows NSIS/MSI 安装包与 Tauri 签名自动更新流程。
- 改进客户端连接、会话恢复与本地安全存储。
